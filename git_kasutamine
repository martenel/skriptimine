root kasutajana:
installi - apt-get install git

tava kasutajana:
esmakordselt:

a) seadista - määra kasutajanimi, emailm editor
git config --global user.name "Ees Perenimi"
git config --global user.email kasutaja@nimi
git config --global core.editor nano

b) loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorum git süsteemi jaoks antud kaustas
git init

remote seadistamine:

a) git remote add nimi aadress
git remote add skriptimine https://github.com/kasutajanimi/reponimi.git

b) git remote kontroll
git remote -v

põhikasutamine:

a) pane fail jälgimisele:
git add failinimi

b) lisa kirjelduse muudatuse kohta
git commit -, "lühikirjelduse lause"

c) lisa kaugreporsitoorimi
git push -uf skriptimine master
